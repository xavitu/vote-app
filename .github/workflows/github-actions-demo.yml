name: Sysdig Scan

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  sysdig_scan:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Sysdig Secure Inline Scan
      uses: sysdiglabs/scan-action@v3.5.0
      with:
        image-tag: "dockersamples/example-voting-app:latest"
        sysdig-secure-token: 6a292fb3-981c-4b45-98df-b54365b6ae34
